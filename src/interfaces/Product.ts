export interface Product {
  name: string;
  description: string;
  imagesUrl: string[];
  category: string;
  price: string;
  discountPercentage: number;
  quantity: number;
  finalPrice: string;
}
