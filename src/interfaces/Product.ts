export interface Product {
  name: string;
  description: string;
  imageUrl: string;
  category: string;
  price: string;
  discountPercentage: number;
  quantity: number;
  finalPrice: string;
}
